name: 'cricket_dbt'
version: '1.0.0'
config-version: 2
profile: 'cricket_dbt'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
seed-paths: ["seeds"]
analysis-paths: ["analyses"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  cricket_dbt:
    bronze:
      +materialized: view
      +schema: BRONZE
    silver:
      +materialized: table
      +schema: SILVER
    gold:
      +materialized: table
      +schema: GOLD

snapshots:
  cricket_dbt:
    +target_schema: SILVER
    +strategy: timestamp
    +updated_at: UPDATED_AT
